#include <bits/stdc++.h>
#define rep(i,j) for (int (i)=0;(i)<(int)(j);++(i))
#define put(i) cout << (i) << endl
using namespace std;
typedef long long ll;
const int mod = 1e9+7;

int main() {
    int a,b,h; cin >> a >> b >> h;
    cout << (a+b)*h/2 << endl;
}
