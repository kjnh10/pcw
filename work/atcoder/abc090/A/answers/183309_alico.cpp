#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
	ios::sync_with_stdio(0);
	
	string s;
	cin >> s; cout << s[0];
	cin >> s; cout << s[1];
	cin >> s; cout << s[2];
	
	return 0;
}